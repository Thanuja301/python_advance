import random
a = int(input("Enter no of teams: "))
teams = []
print("Enter all teams name: ")
for i in range(a):
    t = input("Enter Team name: ")
    teams.append(t)
m = int(input("Enter no of meet btw teams : "))
matches=[]
for i in range(0,a-i):
    for j in range(i+1,a):
        for k in range(m):
            matches.append([teams[i],teams[j]])
random.shuffle(matches)
pos = 1
for i in matches:
    print("Match {} : {} vs {}".format(pos,i[0],i[1]))
    pos = pos+1
